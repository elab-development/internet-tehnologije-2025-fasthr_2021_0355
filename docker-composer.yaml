version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: fast_hr_mysql
    environment:
      MYSQL_DATABASE: fast_hr_db
      MYSQL_ALLOW_EMPTY_PASSWORD: "yes"
      MYSQL_ROOT_HOST: "%"
    command: --default-authentication-plugin=mysql_native_password
    ports:
      - "3306:3306"
    volumes:
      - fast_hr_mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -uroot --silent"]
      interval: 5s
      timeout: 5s
      retries: 30

  backend:
    build:
      context: ./fast-hr-back
      dockerfile: Dockerfile
    container_name: fast_hr_api
    depends_on:
      mysql:
        condition: service_healthy
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_URL: http://localhost:8000

      DB_CONNECTION: mysql
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: fast_hr_db
      DB_USERNAME: root
      DB_PASSWORD: ""

      SESSION_DRIVER: database
      CACHE_STORE: database
      QUEUE_CONNECTION: database
    ports:
      - "8000:8000"

  frontend:
    build:
      context: ./fast-hr-ui
      dockerfile: Dockerfile
    container_name: fast_hr_frontend
    depends_on:
      - backend
    environment:
      CHOKIDAR_USEPOLLING: "true"
      HOST: "0.0.0.0"
      REACT_APP_API_BASE_URL: "http://localhost:8000/api"
    ports:
      - "3000:3000"

volumes:
  fast_hr_mysql_data:
